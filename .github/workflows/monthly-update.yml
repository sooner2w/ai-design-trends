- name: Commit updated files
        run: |
          git config user.name "AlphaStack Bot"
          git config user.email "bot@alphastackapp.com"
          git pull --rebase
          git add -A
          git diff --staged --quiet || git commit -m "Monthly content refresh"
          git push

